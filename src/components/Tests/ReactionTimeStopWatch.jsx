import React from 'react';

const ReactionTimeStopWatch = (props) => {
    return (  
        <div>
            <h2>{props.reactionTime}</h2>
        </div>
    );
}
 
export default ReactionTimeStopWatch;